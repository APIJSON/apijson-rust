# Panda Base 多数据源配置示例
# 请根据实际环境修改连接信息

datasource:
  # MySQL 数据源配置
  - name: ds_mysql
    kind: mysql
    username: root
    password: apijson
    url: "mysql://root:apijson@localhost:3306"
    database: ["sys"]
    default: true
    
#  # PostgreSQL 数据源配置
#  - name: ds_pg
#    kind: postgres
#    username: root
#    password: apijson
#    url: "postgres://root:apijson@localhost:5432/postgres"
#    database: ["sys"]
#    default: false